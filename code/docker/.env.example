# Copy to .env and edit. Values here are the defaults baked into docker-compose.yml.
# You can leave them as-is for local development; just make sure not to expose
# the InfluxDB port (8086) outside localhost.

INFLUX_USERNAME=admin
INFLUX_PASSWORD=changeme-please
INFLUX_ORG=sb20proxy
INFLUX_BUCKET=captures
INFLUX_TOKEN=dev-token-change-me
